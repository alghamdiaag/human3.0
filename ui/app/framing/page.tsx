import { AppShell } from "@/components/app-shell";
import { FramingContent } from "@/components/framing-content";

export default function FramingPage() {
  return (
    <AppShell>
      <FramingContent />
    </AppShell>
  );
}